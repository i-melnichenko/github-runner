# GitHub repository or organization URL to attach the runner to
# Example: https://github.com/your-org/your-repo
GITHUB_URL=https://github.com/your-org/your-repo

# Short-lived registration token generated in GitHub settings
RUNNER_TOKEN=replace_with_runner_registration_token

# Visible runner name in GitHub Actions settings
RUNNER_NAME=github-runner-01

# Runner job workspace inside the container
RUNNER_WORKDIR=/runner/_work

# Optional custom labels (tags), comma-separated
# Example: github,arm64,prod
RUNNER_LABELS=

# Optional explicit path to runner files inside container
# Example: /home/runner
RUNNER_DIR=
